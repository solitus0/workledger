interface:
  display_name: "Workledger Onboarding"
  short_description: "Guide first-time workledger setup, adapter setup, config inspection, env vars, and diagnostics"
  default_prompt: "Use $workledger-onboarding to initialize workledger, configure Jira or Clockify adapters, prepare token environment variables, inspect configuration with workledger config, and run comprehensive setup diagnostics with workledger status."
