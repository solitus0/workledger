# Local worklog batch planning tasks

- Implemented in the current codebase: `worklogs shift` dry-run preview and atomic timestamp movement.
- Implemented in the current codebase: `worklogs apply` raw payload validation and atomic batch add execution.
- Implemented in the current codebase: `worklogs apply --dry` deterministic preview output.
- Keep batch-planning docs and examples aligned around `context` plus external payload construction.
