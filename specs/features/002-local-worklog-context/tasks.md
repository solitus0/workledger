# Local worklog context tasks

- Implement `worklogs context` day snapshots over canonical local worklogs.
- Implement free-slot and collision analysis using effective workday settings, including prorated cross-midnight occupancy inside each selected local day.
- Implement repeated `--issue` planning input plus planning-hint JSON output with required baseline fields and additive metadata support.
- Implement `table` and `json` output with stable planning-state summaries.
